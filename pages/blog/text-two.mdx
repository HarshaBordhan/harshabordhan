# This is second post

import First from './text.mdx';

<First />

export default ({ children }) => (
  <article className="prose prose-quoteless prose-neutral dark:prose-invert">
    {children}
  </article>
);
